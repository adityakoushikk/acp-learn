# Direct production dependencies. Transitive versions are resolved by pip and
# audited during deployment; run `pip-audit -r requirements.txt` before release.
Flask==3.1.3
flask-cors==6.0.5
gunicorn==26.1.0
numpy==2.3.5
pandas==3.0.5
tensorflow==2.21.0
